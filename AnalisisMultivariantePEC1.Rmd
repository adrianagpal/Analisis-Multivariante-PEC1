---
title: "AnalisisMultivariantePEC1"
output:
  pdf_document:
    toc: true
    toc_depth: '3'
    latex_engine: lualatex
  html_document:
    toc: true
    toc_depth: 3
    toc_float:
      collapsed: true
      smooth_scroll: true
    highlight: textmate
---

```{r warning=FALSE, message=FALSE}
library(dplyr)
library(ggplot2)
library(factoextra)
library(labdsv)
library(vegan)
library(MASS)
library(ca)
library(ggpubr)
library(ggrepel)
library(ade4)

set.seed(4567)

knitr::opts_chunk$set(fig.width=6, fig.height=3, fig.align="center")
```

# Ejercicio 1. Antes de empezar el análisis realizaremos una estadística descriptiva inicial.

**(a) Los datos son recuentos de 0 a 32. Calcular los totales por virus que llamaremos total_residue. Dar el recorrido de esta variable.**
```{r warning=FALSE, message=FALSE}
load(file = "virus.rda")

# Calculamos totales por virus
(total_residue <- rowSums(virus[1:(ncol(virus) - 1)]))

# Recorrido variable
range(total_residue)

range(total_residue)[2] - range(total_residue)[1] # max - min
```

**(b) Calcular las medias para cada aminoácido.**
```{r}
round(colMeans(virus[1:(ncol(virus) - 1)]), 3)
```

**(c) Dibujar el histograma de la figura 3. Para ello habrá que fijar las marcas de clase en algunos valores como 120, . . . , 160, . . . , 180, . . . y además un ancho de barra de 5. Cualquier otra configuración distorsiona el histograma que resulta un gráfico poco robusto.**
```{r}
df <- data.frame(total_residue, "virus_group" = virus$type)

ggplot(df, aes(x = total_residue)) +
  geom_histogram(color="white", fill = "darkgray", binwidth = 5) +
  theme_classic() +
  scale_x_continuous(breaks = c(120, 140, 160, 180, 200, 220, 240)) +
  coord_cartesian(xlim = c(120, 240)) +
  scale_y_continuous(breaks = c(0, 5, 10, 15, 20, 25)) +
  xlab("total residue") + theme(axis.title.y=element_blank())
```

**(d) Dibujar el diagrama de cajas (boxplot) de la figura 3. Identificar el valor atípico más destacado del grupo de los Furoviruses.**
```{r}
ggplot(df, aes(x = virus_group, y = total_residue)) + 
  geom_boxplot(outlier.shape = "-") +
  theme_classic() +
  xlab("virus group") + ylab("total residue")
```

**(e) Dividir la variable total_residue en una partición de cuatro intervalos de forma que podamos clasificar los tipos de virus perfectamente, excepto un único virus extraño.**
```{r}
categories <- unique(df$virus_group)

for (category in categories){
  print(category)
  print(range(df$total_residue[df$virus_group == category]))
}

breaks <- c(120, 165, 185, 195, 240)

df$class <- cut(df$total_residue,
                breaks = breaks)
```

# Ejercicio 2
**En este apartado y los siguientes vamos a trabajar únicamente con los Tobamoviruses. Recordar que hay dos virus con idénticas puntuaciones y únicamente incluiremos uno. Luego el primer paso es identificar y eliminar uno de los virus.**

**Realizar un análisis de componentes principales con los n = 38 virus del grupo Tobamovirus con las p = 18 características. Considerar las variables “tal cual” y repetir el análisis con las variables escaladas. Utilizar un máximo de 6 componentes. Realizar los gráficos y todas las explicaciones sobre este análisis.**

```{r fig.width=8, fig.height=5}
# Seleccionamos tobamoviruses
tobamovirus <- virus[which(virus$type == "Toba"),]

# Eliminamos duplicado
tobamovirus <- tobamovirus %>% distinct()

# Seleccionar matriz de datos numéricos
toba_num <- select_if(tobamovirus, is.numeric)

# Calcular desviación estándar por columna
apply(toba_num, 2, sd)

# PCA con variables sin escalar ni centrar
virus.pca.noscale <- prcomp(toba_num, scale = FALSE, center = FALSE)

# Proporción de la varianza total explicada por cada componente
fviz_eig(virus.pca.noscale, addlabels = TRUE, ncp = 6, main = "% Variation") + ylim(0, 100)

summary(virus.pca.noscale)

# Contribución (%) de cada variable a cada componente
var <- get_pca_var(virus.pca.noscale)

var$contrib[,1:2]

# Gráfico de variables
g1 <- fviz_pca_var(virus.pca.noscale,
             col.var = "contrib", 
             gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
             repel = TRUE)

# Diagrama de dispersión formado por los dos primeros ejes principales
g2 <- fviz_pca_ind(virus.pca.noscale,
             col.ind = "cos2", 
             gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
             repel = TRUE)

# Combinar en un solo panel
ggarrange(g1, g2, ncol = 2, nrow = 1)

(eig.val <- get_eigenvalue(virus.pca.noscale))

# PCA con variables escaladas y centradas
virus.pcscale <- prcomp(toba_num, scale = TRUE, center = TRUE)

# Proporción de la varianza total explicada por cada componente
fviz_eig(virus.pcscale, addlabels = TRUE, ncp = 6, main = "% Variation") + ylim(0, 30)

summary(virus.pcscale)

# Contribución (%) de cada variable a cada componente
var <- get_pca_var(virus.pcscale)

var$contrib[,1:2]

# Gráfico de variables
g3 <- fviz_pca_var(virus.pcscale,
             col.var = "contrib", 
             gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
             repel = TRUE)

# Diagrama de dispersión formado por los dos primeros ejes principales
g4 <- fviz_pca_ind(virus.pcscale,
             col.ind = "cos2", 
             gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
             repel = TRUE)

# Combinar en un solo panel
ggarrange(g3, g4, ncol = 2, nrow = 1)

(eig.val <- get_eigenvalue(virus.pcscale))
```

El hecho de que un único componente principal explique casi toda la varianza total hace pensar que las variables son colineales. 

**¿Cuales son las variables con mayor contribución a los dos primeros ejes en los dos análisis?**
En el análisis de componentes principales con las variables sin centrar ni escalar, la mayor parte de la varianza total es explicada por un único componente, que explica el 96.1%. Las variables que contribuyen en mayor medida son V1, V2, V3, V4 y V7. 

En el PCA con las variables centradas y escaladas, se necesitan cinco componentes principales para superar el 80% de la variabilidad explicada. Las variables con mayor contribución son V4, V8, V10, V13 y V14. 

**¿Se observan algunas agrupaciones de los virus? ¿Cuantas?**

En el primer análisis, se observan dos agrupaciones, conteniendo una de ellas la mayoría de los individuos. En el segundo análisis, se observan tres agrupaciones. 

**Identificar el virus que está entre dos grupos. Utilizar como etiquetas las originales de la base de datos completa. Ajustar el tamaño de las etiquetas para que sean visibles en el gráfico de dispersión.**

El individuo 11 se encuentre entre dos agrupaciones. Esto es visible únicamente en el PCA con datos centrados y escalados. 

## Ejercicio 3
**Realizar un análisis MDS clásico con la distancia euclídea entre los virus del grupo Tobamovirus y con las variables sin escalar.**
```{r}
# Calcular matriz de distancias euclídeas
D <- as.matrix(dist(toba_num))

rownames(D) <- rownames(D)
```

**Dibujar el gráfico de dispersión en dos dimensiones y valorar el resultado.**
```{r}
# Análisis MDS clásico
mds <- cmdscale(D, k=5, eig=T); mds

# Autovalores asociados a las dimensiones del MDS
(lam <- mds$eig)

# Bondad de ajuste
sum(lam[1:2])/sum(lam[lam>0])

cumsum(abs(lam)) / sum(abs(lam))
cumsum(lam^2) / sum(lam^2)

pb <- cumsum(lam) / sum(lam) * 100

# Porcentaje acumulado de cobertura
names(pb) <- paste("PC",1:10)
barplot(pb[1:9], ylab="porcentaje", ylim=c(0,100))
title(main="Porcentaje acumulado de cobertura",line=1.5)

plot(mds$points^2, type="n", xlab="Coordinate 1", ylab="Coordinate 2")
abline(h=0, v=0, lty=4, col="gray")
text(mds$points[,1], mds$points[,2], cex=0.8)
title("MDS con distancia euclídea")

n <- nrow(as.matrix(D))
unos <- rep(1,n)
I <- diag(unos)
H <- I - 1/n*(unos %*% t(unos))
B <- -1/2*(H %*% as.matrix(D) %*% H)
rownames(B) <- rownames(as.matrix(D))
colnames(B) <- colnames(as.matrix(D))
evv <- eigen(B)$values; evv

eigen(B)$values

B.eigen <- eigen(B)

sum(B.eigen$val[1:18])/sum(B.eigen$val[B.eigen$val>0])

plot(mds$points,type="n",xlab="PC1",ylab="PC2")
abline(h=0,v=0,lty=4,col="gray")
text(mds$points[,1], mds$points[,2],labels=rownames(D),cex=0.8)

plot(mds$points[,2:3],type="n",xlab="PC2",ylab="PC3")
abline(h=0,v=0,lty=4,col="gray")
text(mds$points[,2], mds$points[,3],labels=rownames(D),cex=0.8)

plot(mds$points[,3:4],type="n",xlab="PC3",ylab="PC4")
abline(h=0,v=0,lty=4,col="gray")
text(mds$points[,3], mds$points[,4],labels=rownames(D),cex=0.8)

plot(mds$points[,4:5],type="n",xlab="PC4",ylab="PC5")
abline(h=0,v=0,lty=4,col="gray")
text(mds$points[,4], mds$points[,5],labels=rownames(D),cex=0.8)
```

Si comprobamos cuántas coordenadas necesitamos para representar adecuadamente la matriz de distancias, los valores sugieren que necesitamos al menos 5 coordenadas. Necesitamos al menos 4 coordenadas para cubrir el 80% de la variabilidad. Las agrupaciones no se conservan bien cuando se representan las distintas coordenadas.
Los valores sugieren que las primeras dos coordenadas no ofrecen una representación adecuada de las distancias observadas. 

**Uno de los objetivos del estudio es hallar virus aislados o “alejados de cualquier grupo”. Para ello podemos utilizar el gráfico mean vs minimum de la función disana() del paquete labdsv. Los puntos más a la derecha y arriba nos indican observaciones alejadas de cualquier grupo.**
```{r}
outliers <- disana(D, panel = 3)

merge(outliers$min, outliers$mean, by = "row.names") %>%
  rename(min = x, mean = y) %>%
  ggplot(aes(x = min, y = mean, label = Row.names)) +
    theme(plot.title = element_text(hjust = 0.5)) +
    xlab("Minimum Dissimilarity") +
    ylab("Average Dissimilatity") +
    geom_point() +
    geom_text_repel(size = 3) +
theme_bw()
```

## Ejercicio 4
**Como las variables son puntuaciones no continuas, la distancia euclídea no parece la mejor elección. Entre las alternativas podemos considerar la distancia de Bray-Curtis. Realizar un MDS no métrico de Kruskal con la distancia de Bray-Curtis entre los virus del grupo Tobamovirus.**
```{r}
# Calcular matriz de distancias de Bray-Curtis
dist.bc <- vegdist(toba_num, "bray")

# Calcular NMDS 
nmds.iso <- isoMDS(dist.bc, trace = FALSE)

nmds.iso$stress

# Assess the goodness of fit
spe.nmds <- metaMDS(toba_num, distance = "bray", k = 2)
spe.nmds

stressplot(spe.nmds, main = "Shepard plot")
```

The correlation between observed dissimilarity and ordination distance (R2 > 0.95) and the stress value relatively low, showing together a good accuracy of the NMDS ordination.

**¿Por qué debemos realizar un MDS no métrico, mejor que un MDS métrico clásico?**
```{r}
is.euclid(dist.bc, print=T)
```

El MDS no métrico se basa en distancias u órdenes de rango en lugar de en distancias euclídeas. El MDS clásico asume que la matriz de distancias proviene de un espacio euclídeo. Sin embargo, al comprobar si dicha matriz es euclídea, vemos que no lo es. Por tanto, la representación de los puntos puede no mostrar adecuadamente las distancias entre los individuos. 

**Dibujar un scree plot sobre el stress para evaluar el número de ejes a considerar. Dibujar también un gráfico de Shepard para comparar las distancias originales y las propuestas por el MDS no métrico con 2 dimensiones. ¿Cuantos subgrupos vemos en el gráfico de dispersión con 2 dimensiones?**
```{r}
NMDS.scree <- function(x) { plot(rep(1, 10), replicate(10, 
          metaMDS(x, autotransform = F, k = 1)$stress),  
          xlim = c(1, 10),ylim = c(0, 0.30), xlab = "# of Dimensions",  
          ylab = "Stress", main = "NMDS stress plot") 
  for (i in 1:10) {  points(rep(i + 1,10),replicate(10, 
          metaMDS(x, autotransform = F, k = i + 1)$stress)) } }

invisible(capture.output({
  NMDS.scree(dist.bc)
}))
  
  
n <- nrow(as.matrix(dist.bc))
unos <- rep(1, n)
I <- diag(unos)
H <- I - 1/n*(unos %*% t(unos))
B <- -1/2*(H %*% as.matrix(dist.bc) %*% H)
rownames(B) <- rownames(as.matrix(dist.bc))
colnames(B) <- colnames(as.matrix(dist.bc))
evv <- eigen(B)$values; evv

pb <- evv[1:10]/sum(evv[1:10])*100; round(pb,2)

names(pb) <- paste("PC",1:10)
barplot(pb, ylab="porcentaje")
title(main="Porcentaje acumulado de cobertura", line=1.5)

stress_values <- numeric(10)
for (k in 1:10) {
  fit <- isoMDS(dist.bc, k = k, trace = FALSE)
  stress_values[k] <- fit$stress
}

plot(1:10, stress_values, type = "b", pch = 19,
     xlab = "Número de dimensiones (k)", ylab = "Stress",
     main = "Scree plot - MDS no métrico (Bray-Curtis)")
abline(h = 0.2, col = "red", lty = 2)  # línea de referencia
```

```{r}
scree.plot = function(d, k) {
    stresses=isoMDS(d, k=k)$stress
    for(i in rev(seq(k-1)))  
        stresses=append(stresses,isoMDS(d, k=i)$stress)
    plot(seq(k),rev(stresses), type="b", xaxp=c(1,k, k-1), ylab="Stress", xlab="Number of dimensions")
    }

scree.plot(dist.bc, k=6)

nmds.sh <- Shepard(dist.bc, nmds.iso$points)
plot(nmds.sh, pch="x")
```

```{r}
# Plot NMDS
data.iwr = as.data.frame(scores(spe.nmds)$sites)

species.iwr = as.data.frame(scores(spe.nmds)$species)

species.iwr$species <- rownames(species.iwr)

ggplot() + 
  geom_text(data = species.iwr, aes(x = NMDS1, y = NMDS2, label = species)) + 
  geom_point(data = data.iwr, aes(x = NMDS1, y = NMDS2)) +  
  annotate(geom = "label", x = 0.5, y = 1.25, size = 5, fill="lightgrey",
           label = paste("Stress: ", round(spe.nmds$stress, digits = 3))) +
  theme_bw() + 
  theme(legend.position = "right", text = element_text(size = 15) ) + 
  labs(colour="Site")


### Construct the biplot
plot(spe.nmds, type = "none", main = paste("NMDS/Bray - Stress=",
    round(spe.nmds$stress, 3)), xlab = c("NMDS1"), ylab = c("NMDS2"))
points(scores(spe.nmds, display = "sites", choices = c(1,
    2)), pch = 21, col = "black", bg = "steelblue", cex = 1.2)
text(scores(spe.nmds, display = "species", choices = c(1)),
    scores(spe.nmds, display = "species", choices = c(2)),
    labels = rownames(scores(spe.nmds, display = "species")),
    col = "red", cex = 0.8)
```



## Ejercicio 5
**Realizar un mapa no lineal de Sammon, es decir, otro tipo de MDS no métrico con la distancia de Bray-Curtis y los datos de Tobamovirus. Dibujar los gráficos del apartado anterior y responder las mismas cuestiones.**
```{r}
nmds.sa <- sammon(dist.bc, trace = FALSE)

eqscplot(nmds.sa$points, type="n")
text(nmds.sa$points, cex=.8)
```

## Ejercicio 6
**Por último vamos a utilizar una técnica que pretende hallar la proyección más “interesante” de un conjunto multivariante de datos. Se trata de la llamada projection pursuit (PP).**
```{r}
library(Pursuit)

invisible(capture.output({
  pp_ft <- PP_Optimizer(tobamovirus[1:(ncol(tobamovirus) - 1)], findex = "friedmantukey")
  }))

invisible(capture.output({
  pp_ht <- PP_Optimizer(tobamovirus[1:(ncol(tobamovirus) - 1)], findex = "hermite")
  }))

plot(pp_ft$proj.data)

plot(pp_ht$proj.data)
```
## Ejercicio 7
**Otra distancia que se utiliza mucho con puntuaciones o frecuencias es la distancia ji-cuadrado. Comprobar que la matriz D de esta distancia sobre los datos de Tobamovirus verifica que**

$$
B = −1/2  H(D × D)H
$$
**es semidefinida positiva y, por lo tanto, la distancia es euclídea. La matriz H es la matriz de centrado. Realizar un MDS con esta distancia y comentar el resultado respecto a los ejercicios anteriores.**

```{r}
library(svs)

dist.chisq <- dist_chisquare(as.matrix(tobamovirus[1:(ncol(tobamovirus) - 1)]), diag = TRUE, upper = TRUE)

n <- nrow(as.matrix(dist.chisq))
unos <- rep(1,n)
I <- diag(unos)
H <- I - 1/n*(unos %*% t(unos))
B <- -1/2*(H %*% as.matrix(dist.chisq) %*% H)
rownames(B) <- rownames(as.matrix(dist.chisq))
colnames(B) <- colnames(as.matrix(dist.chisq))
evv <- eigen(B)$values; evv

is.euclid(as.dist(dist.chisq))
```
```{r}
# Análisis MDS
mds <- cmdscale(dist.chisq, eig=T); mds

plot(mds$points,type="n",xlab="PC1",ylab="PC2")
abline(h=0,v=0,lty=4,col="gray")
text(mds$points[,1], mds$points[,2],cex=0.8)
title("MDS con distancia euclídea")
```

## Ejercicio 8
**Realizar un análisis de correspondencias. Calcular la inercia total con los valores propios y las primeras inercias principales (en %). Dibujar una representación simétrica del CA y una representación solo de los virus. Comparar el resultado con el obtenido con la distancia ji-cuadrado.**
```{r}
as.table(as.matrix(tobamovirus[1:(ncol(tobamovirus) - 1)]))



tabla.N <- as.table(as.matrix(tobamovirus[1:(ncol(tobamovirus) - 1)]))

tabla.F <- tabla.N/ sum(tabla.N)

# Frecuencias relativas marginales.
margin.f <- apply(tabla.F,1,sum)
margin.c <- apply(tabla.F,2,sum)

# Frecuencias relativas condicionadas por filas.

tabla.P <- sweep(tabla.F, 1, margin.f, "/")

nf <- nrow(tobamovirus[1:(ncol(tobamovirus) - 1)])

D2.chisq <- matrix(0,nf,nf)

for(i in 1:(nf-1))
  for(j in i:nf)
    D2.chisq[i,j] <- t(tabla.P[i,]-tabla.P[j,]) %*% diag(1/margin.c) %*% (tabla.P[i,]-tabla.P[j,])

dc <- vector(mode="numeric", nf)

for(i in 1:nf)
  dc[i] <- t(tabla.P[i,]-margin.c) %*% diag(1/margin.c) %*% (tabla.P[i,]-margin.c)
names(dc) <- rownames(tabla.N); dc

as.numeric(dc %*% margin.f)

# Inercia total
as.numeric(chisq.test(tabla.N)$stat)/sum(tabla.N)

sum(ca(tabla.N)$rowinertia)

plot(ca(tabla.F))
title(main="Solución simétrica", line=1)


Z <- diag(1/sqrt(margin.f)) %*% (tabla.F - margin.f %*% t(margin.c)) %*% diag(1/sqrt(margin.c))
                                                            
Z.svd <- svd(Z); Z.svd

f.sc <- diag(1/sqrt(margin.f)) %*% Z.svd$u
c.sc <- diag(1/sqrt(margin.c)) %*% Z.svd$v
f.pc <- f.sc %*% diag(Z.svd$d)
c.pc <- c.sc %*% diag(Z.svd$d)                   
                                                            library(MASS)        # Mapa de distancias con filas y columnas                
eqscplot(f.pc[,1:2],type="n",xlab="PC1",ylab="PC2",xlim=c(-1,1.2))
abline(v=0,h=0, col="gray",lty=4)
text(f.pc[,1],f.pc[,2],labels=rownames(tabla.F),cex=0.8,font=2,col="blue")
text(c.pc[,1],c.pc[,2],labels=colnames(tabla.F),cex=0.8,font=2,col= "red")
title(main="Solución simétrica",line=1)
```

## Ejercicio 9
**Realizar un brevísimo informe con las conclusiones sobre la formación de grupos con los datos de Tobamovirus que se deducen de los ejercicios anteriores.**

## Bibliografía

https://rpubs.com/YaPi/393252 [fecha de consulta: 15/04/2025]

https://rpubs.com/robinfromthahood/NMDS [fecha de consulta: 16/04/2025] NMDS

https://r.qcbs.ca/workshop09/book-en/nonmetric-multidimensional-scaling.html [fecha de consulta: 16/04/2025] NMDS